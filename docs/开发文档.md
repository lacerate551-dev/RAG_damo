# RAG 知识库问答系统 - 开发文档

> **项目版本**: v6.0.0
> **更新日期**: 2026-04-13
> **文档用途**: 架构说明、技术栈、部署指南

> **API 接口文档**: 详见 [API接口文档.md](./API接口文档.md)

---

## 一、项目概述

### 1.1 项目定位

本项目是智能出题系统的**核心知识服务层**，为上层 Dify 工作流提供知识检索能力。系统通过 RAG（检索增强生成）技术，实现基于企业制度文档的智能问答，支持：

- **知识库问答**：基于向量检索 + BM25 + Rerank 的混合检索
- **图谱推理**：基于 Neo4j 的多跳关系查询
- **网络搜索**：实时信息获取（天气、新闻等）
- **Dify 集成**：HTTP API 供工作流调用

### 1.2 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                          前端应用层                                  │
│                    (独立开发，本项目不涉及)                           │
└───────────────────────────────┬─────────────────────────────────────┘
                                │ HTTP API
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       API 服务层 (本项目)                            │
│                         main.py (入口)                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │   /chat     │  │    /rag     │  │ /graph/*    │  │  /search   │ │
│  │ 智能聊天    │  │ 知识库问答  │  │ 图谱检索    │  │ 混合检索   │ │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └─────┬──────┘ │
└─────────┼────────────────┼────────────────┼───────────────┼────────┘
          │                │                │               │
          ▼                ▼                ▼               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       核心能力层                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                     Agentic RAG                              │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌──────────┐ │   │
│  │  │Agent决策  │  │知识库检索 │  │ 网络搜索  │  │ 图谱检索 │ │   │
│  │  │检索/改写  │  │向量+BM25  │  │ Serper API│  │ Neo4j   │ │   │
│  │  │分解/回答  │  │ +Rerank   │  │           │  │         │ │   │
│  │  └───────────┘  └───────────┘  └───────────┘  └──────────┘ │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       数据存储层                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │  ChromaDB   │  │   Neo4j     │  │  SQLite     │  │ documents/ │ │
│  │  向量数据库  │  │  知识图谱   │  │  业务数据   │  │  文档源    │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       外部服务层                                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────┐ │
│  │ Qwen API    │  │ Serper API  │  │      Dify 工作流            │ │
│  │ 大模型服务  │  │ 网络搜索    │  │  出题 / 批阅 / 其他工作流   │ │
│  └─────────────┘  └─────────────┘  └─────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.3 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| API 服务 | Flask + Flask-CORS | RESTful API，支持跨域，应用工厂模式 |
| 向量检索 | ChromaDB + BGE-base-zh | 本地向量数据库 + 嵌入模型 |
| 关键词检索 | BM25 + jieba | 中文分词 + 倒排索引 |
| 重排序 | BGE-reranker-base | CrossEncoder 精排 |
| 知识图谱 | Neo4j | 实体关系存储 |
| 大模型 | Qwen3.5 (通义千问) | 问答生成、实体提取 |
| 网络搜索 | Serper API | Google 搜索接口 |
| 数据库 | SQLite | 统一数据访问层 (data/db.py) |

---

## 二、项目结构（模块化）

```
├── main.py                  # ✨ 统一启动入口（推荐）
├── config.py                # API 配置（需自行创建）
├── config.example.py        # API 配置模板
├── requirements.txt         # 依赖列表
│
├── api/                     # API 路由层（Flask Blueprint）
│   ├── __init__.py          #   create_app() 应用工厂
│   ├── chat_routes.py       #   /chat, /rag, /rag/stream, /search
│   ├── session_routes.py    #   /sessions, /history
│   ├── auth_routes.py       #   /stats, /health, /auth/me
│   ├── kb_routes.py         #   /collections, /kb/route
│   ├── document_routes.py   #   /documents/upload, 版本管理
│   ├── sync_routes.py       #   /sync, /subscribe
│   ├── graph_routes.py      #   /graph/search, /graph/build
│   ├── feedback_routes.py   #   /feedback, /faq
│   └── ...                  #   其他路由
│
├── core/                    # RAG 核心引擎
│   ├── agentic.py           #   AgenticRAG 智能问答
│   ├── engine.py            #   检索引擎封装
│   ├── bm25_index.py        #   BM25 索引
│   └── chunker.py           #   语义分块器
│
├── knowledge/               # 知识库管理
│   ├── manager.py           #   多向量库管理器
│   ├── router.py            #   知识库路由器
│   └── sync.py              #   同步服务
│
├── exam_pkg/                # 考试系统
│   ├── manager.py           #   出题与批卷
│   ├── api.py               #   Flask Blueprint
│   └── local_db.py          #   本地题库
│
├── services/                # 业务服务
│   ├── session.py           #   会话管理
│   ├── audit.py             #   审计日志
│   ├── feedback.py          #   反馈闭环
│   └── outline.py           #   纲要生成
│
├── auth/                    # 认证与安全
│   ├── gateway.py           #   网关认证
│   └── security.py          #   安全防护
│
├── data/                    # SQLite 数据库
│   ├── db.py                #   统一数据访问层
│   ├── rag_core.db          #   核心数据
│   ├── knowledge.db         #   知识管理
│   └── exam.db              #   出题系统
│
└── graph/                   # 知识图谱
    ├── graph_manager.py     #   Neo4j 管理
    ├── entity_extractor.py  #   实体提取
    └── graph_rag.py         #   图谱检索
```

---

## 三、开发进度

### 3.1 版本里程碑

| 版本 | 发布日期 | 主要功能 | 状态 |
|------|----------|----------|------|
| v1.0.0 | 2025-03-26 | RAG 本地知识库问答系统 | ✅ 已发布 |
| v2.1.0 | 2025-03-27 | Dify 智能出题系统集成 | ✅ 已发布 |
| v3.0.0 | 2025-03-28 | 双模式对话 + 会话管理 | ✅ 已发布 |
| v4.0.0 | 2025-03-30 | Graph RAG + 网络搜索 | ✅ 已发布 |
| v5.0.0 | 2025-04-12 | 多向量库权限控制 | ✅ 已发布 |
| v6.0.0 | 2025-04-13 | 模块化重构 + 统一数据层 | ✅ 已发布 |

### 3.2 功能完成度

| 功能模块 | 完成度 | 说明 |
|----------|--------|------|
| 文档解析 | 100% | PDF/Word/Excel/TXT |
| 向量检索 | 100% | ChromaDB + BGE |
| BM25 检索 | 100% | 中文分词 + 倒排索引 |
| Rerank 重排 | 100% | CrossEncoder |
| 知识图谱 | 100% | Neo4j + 实体提取 |
| 网络搜索 | 100% | Serper API |
| Agent 决策 | 100% | 动态检索策略 |
| 会话管理 | 100% | SQLite 持久化 |
| API 接口 | 100% | 12+ Blueprint |
| Dify 集成 | 100% | HTTP 检索节点 |
| 模块化架构 | 100% | 应用工厂模式 |
| 统一数据层 | 100% | data/db.py |

---

## 四、API 接口

> **详细 API 文档**: 详见 [API接口文档.md](./API接口文档.md)

### 核心接口概览

| 接口 | 方法 | 说明 |
|------|------|------|
| `/chat` | POST | 智能聊天（支持网络搜索） |
| `/rag` | POST | 知识库问答（多源检索） |
| `/rag/stream` | POST | 知识库问答（SSE 流式） |
| `/search` | POST | 混合检索（供 Dify 调用） |
| `/graph/search` | POST | 图谱检索 |
| `/exam/*` | - | 出题系统相关接口 |
| `/collections` | GET | 向量库列表 |
| `/sessions` | GET | 会话列表 |
| `/health` | GET | 健康检查 |

---

## 五、运行命令

### 5.1 启动服务

```powershell
# 激活虚拟环境后运行

# ✨ 推荐方式 - 新入口
python main.py                  # 启动 API 服务（端口 5001）
python main.py --port 8080      # 指定端口

# 旧入口（仍可用）
python rag_api_server.py        # 启动 API 服务
```

### 5.2 知识库管理

```powershell
# 构建知识库（首次或完全重建）
python rag_demo.py --rebuild

# 增量同步（添加/删除文档后）
python rag_demo.py --sync

# 查看已索引文档
python rag_demo.py --list

# 构建知识图谱
python -m graph.graph_build --stats
```

---

## 六、配置说明

### 6.1 必需配置

```python
# config.py - 必需配置

# 通义千问 API（必需）
DASHSCOPE_API_KEY = "your-api-key"
DASHSCOPE_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"
DASHSCOPE_MODEL = "qwen3.5-plus"

# 兼容变量
API_KEY = DASHSCOPE_API_KEY
BASE_URL = DASHSCOPE_BASE_URL
MODEL = DASHSCOPE_MODEL
```

### 6.2 可选配置

```python
# config.py - 可选配置

# 网络搜索（可选）
SERPER_API_KEY = "your-serper-api-key"

# Dify 集成（可选）
DIFY_API_URL = "https://api.dify.ai/v1"
DIFY_QUESTION_API_KEY = "your-dify-api-key"
DIFY_GRADE_API_KEY = "your-dify-api-key"

# Graph RAG（可选）
NEO4J_URI = "bolt://localhost:7687"
NEO4J_USER = "neo4j"
NEO4J_PASSWORD = "password123"
USE_GRAPH_RAG = True

# 文档路径
DOCUMENTS_PATH = "./documents"
```

### 6.3 环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| Python | 3.9+ | 运行环境 |
| ChromaDB | 0.4+ | 向量数据库 |
| Neo4j | 5.x | 图数据库（可选） |
| 内存 | 4GB+ | 推荐 8GB |

---

## 七、部署指南

### 7.1 Docker 部署 Neo4j

```bash
docker run -d \
  --name neo4j \
  -p 7474:7474 -p 7687:7687 \
  -e NEO4J_AUTH=neo4j/password123 \
  -v neo4j_data:/data \
  neo4j:latest
```

### 7.2 启动 API 服务

```powershell
# 激活虚拟环境
.\venv\Scripts\Activate.ps1

# 启动服务
python main.py
```

**服务启动日志**
```
✓ 应用初始化完成，共注册 XX 个路由
  核心路由: /chat, /rag, /rag/stream, /search
  会话管理: /sessions, /history, /session, /clear
  系统状态: /health, /stats, /auth/me
  向量库:   /collections, /documents/sync, /kb/route

🚀 RAG API 服务启动: http://0.0.0.0:5001
   调试模式: 开启
```

### 7.3 生产环境建议

| 项目 | 建议 |
|------|------|
| WSGI 服务器 | gunicorn 或 uWSGI |
| 进程数 | 4-8 workers |
| 反向代理 | Nginx |
| HTTPS | 配置 SSL 证书 |
| 日志 | 配置日志轮转 |
| 监控 | Prometheus + Grafana |

---

## 八、Dify 工作流集成

### 8.1 集成架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     Dify 工作流                                  │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐ │
│  │  开始节点   │───▶│  LLM 节点   │───▶│      结束节点       │ │
│  │  (用户问题) │    │  (出题/批阅)│    │    (输出结果)       │ │
│  └─────────────┘    └──────┬──────┘    └─────────────────────┘ │
│                            │                                    │
│                            ▼                                    │
│                    ┌───────────────┐                           │
│                    │   HTTP 节点    │                           │
│                    │  (知识检索)    │                           │
│                    └───────┬───────┘                           │
└────────────────────────────┼────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   RAG API 服务   │
                    │  POST /search    │
                    └─────────────────┘
```

### 8.2 HTTP 节点配置

```yaml
# HTTP 节点配置
URL: http://your-server:5001/search
Method: POST
Headers:
  Content-Type: application/json
  Authorization: Bearer {{user_token}}
Body:
  {
    "query": "{{input_text}}",
    "top_k": 5
  }
```

---

## 九、错误码说明

| 状态码 | 说明 | 处理建议 |
|--------|------|----------|
| 200 | 成功 | - |
| 400 | 请求参数错误 | 检查请求体格式 |
| 401 | 未认证 | 检查 Header 是否包含认证信息 |
| 403 | 权限不足 | 检查用户角色权限 |
| 404 | 资源不存在 | 检查 session_id 或资源路径 |
| 500 | 服务器内部错误 | 查看服务日志 |

---

## 十、相关文档

- [API接口文档.md](./API接口文档.md) - REST API 详细说明
- [数据库设计文档.md](./数据库设计文档.md) - 所有数据库结构
- [模块说明.md](./模块说明.md) - 模块详细说明
- [认证与权限配置指南.md](./认证与权限配置指南.md) - 网关认证说明
- [多向量库实现权限划分.md](./多向量库实现权限划分.md) - 权限架构说明

---

## 附录：接口速查表

| 接口 | 方法 | 用途 | 调用方 |
|------|------|------|--------|
| `/chat` | POST | 智能聊天 | 前端 |
| `/rag` | POST | 知识库问答 | 前端 |
| `/rag/stream` | POST | 知识库问答（流式） | 前端 |
| `/search` | POST | 混合检索 | Dify |
| `/sessions` | GET | 会话列表 | 前端 |
| `/history/{id}` | GET | 会话历史 | 前端 |
| `/session/{id}` | DELETE | 删除会话 | 前端 |
| `/graph/search` | POST | 图谱检索 | 前端/Dify |
| `/graph/stats` | GET | 图谱统计 | 前端 |
| `/collections` | GET | 向量库列表 | 前端 |
| `/health` | GET | 健康检查 | 运维 |
| `/stats` | GET | 系统统计 | 管理后台 |

# 出题批卷功能改造计划

> **文档类型**: 实施计划
> **创建日期**: 2026-04-10
> **状态**: 待实施

---

## 一、改造背景

### 现有问题
1. **出题方式**：当前按"主题"出题，无法精确溯源到具体文件
2. **批卷方式**：依赖本地JSON文件存储试卷，前后端无法共享题目数据
3. **文件修改联动**：文件更新后无法自动识别受影响的题目

### 改造目标
1. **按文件出题**：根据指定文件生成题目，每道题附带来源文件信息
2. **前后端分离**：后端提供出题/批卷API，前端管理MySQL题库
3. **文件修改联动**：文件修改时可通过来源字段找到相关题目，触发重新生成

---

## 二、后端改造方案

### 2.1 新增出题接口

**接口路径**：`POST /exam/generate-by-file`

**请求参数**：
```json
{
    "file_path": "public/产品手册.pdf",
    "collection": "public_kb",
    "choice_count": 5,
    "blank_count": 2,
    "short_answer_count": 2,
    "difficulty": 3,
    "choice_score": 2,
    "blank_score": 3
}
```

**返回结果**：
```json
{
    "success": true,
    "source_file": {
        "path": "public/产品手册.pdf",
        "collection": "public_kb",
        "hash": "abc123..."
    },
    "generated_at": "2026-04-10T14:00:00",
    "questions": {
        "choice_questions": [
            {
                "id": "choice_1",
                "content": "题目内容",
                "options": ["A. 选项1", "B. 选项2", "C. 选项3", "D. 选项4"],
                "answer": "A",
                "analysis": "解析内容",
                "knowledge_points": ["知识点1"],
                "difficulty": 3,
                "score": 2,
                "source_file": "public/产品手册.pdf",
                "source_snippet": "该题依据的知识片段..."
            }
        ],
        "blank_questions": [...],
        "short_answer_questions": [...]
    },
    "total_count": 9,
    "total_score": 22
}
```

### 2.2 改造批卷接口

**接口路径**：`POST /exam/grade-from-mysql`

**设计思路**：
- 前端传入题目ID列表和学生答案
- 后端根据题目ID从MySQL获取正确答案（前端需提供或后端查询）
- 批量批阅后返回结果

**请求参数**：
```json
{
    "exam_id": "试卷ID（前端生成）",
    "student_id": "学生ID",
    "student_name": "张三",
    "answers": [
        {
            "question_id": "q_uuid_001",
            "question_type": "choice",
            "question_content": "题目内容",
            "correct_answer": "A",
            "max_score": 2,
            "student_answer": "B"
        },
        {
            "question_id": "q_uuid_002",
            "question_type": "blank",
            "question_content": "填空题内容______",
            "correct_answer": "正确答案",
            "max_score": 3,
            "student_answer": "学生答案"
        },
        {
            "question_id": "q_uuid_003",
            "question_type": "short_answer",
            "question_content": "简答题内容",
            "correct_answer": "{\"points\":[{\"point\":\"要点1\",\"score\":3}]}",
            "max_score": 10,
            "student_answer": "学生作答内容..."
        }
    ]
}
```

**返回结果**：
```json
{
    "success": true,
    "exam_id": "试卷ID",
    "student_id": "学生ID",
    "student_name": "张三",
    "graded_at": "2026-04-10T15:00:00",
    "total_score": 75,
    "max_score": 100,
    "score_rate": 75.0,
    "results": [
        {
            "question_id": "q_uuid_001",
            "question_type": "choice",
            "score": 0,
            "max_score": 2,
            "correct": false,
            "feedback": "回答错误，正确答案是A"
        },
        {
            "question_id": "q_uuid_002",
            "question_type": "blank",
            "score": 3,
            "max_score": 3,
            "correct": true,
            "feedback": "回答正确！"
        },
        {
            "question_id": "q_uuid_003",
            "question_type": "short_answer",
            "score": 7,
            "max_score": 10,
            "feedback": "整体回答较好，但缺少部分要点",
            "score_details": [
                {"point": "要点1", "earned": 3, "max": 3},
                {"point": "要点2", "earned": 2, "max": 4},
                {"point": "要点3", "earned": 2, "max": 3}
            ],
            "highlights": ["理解准确"],
            "shortcomings": ["缺少实践案例"]
        }
    ]
}
```

### 2.3 新增文件关联查询接口

**接口路径**：`GET /exam/by-file`

**请求参数**：
```
file_path=public/产品手册.pdf
```

**返回结果**：
```json
{
    "success": true,
    "file_path": "public/产品手册.pdf",
    "file_hash": "abc123...",
    "exists": true,
    "last_modified": "2026-04-10T12:00:00"
}
```

---

## 三、代码修改清单

### 3.1 exam_manager.py 修改

| 函数 | 修改内容 |
|------|---------|
| `generate_exam_by_file()` | 新增：按文件生成题目 |
| `grade_from_mysql()` | 新增：基于MySQL题目批卷 |
| `grade_question()` | 修改：支持传入完整题目信息 |

### 3.2 exam_api.py 修改

| 接口 | 修改内容 |
|------|---------|
| `/generate-by-file` | 新增：按文件出题接口 |
| `/grade-from-mysql` | 新增：基于MySQL批卷接口 |
| `/by-file` | 新增：文件关联查询接口 |

### 3.3 Dify工作流修改

**出题工作流输入变更**：
```
原: topic, choice_count, blank_count, short_answer_count, difficulty
新: file_path, collection, choice_count, blank_count, short_answer_count, difficulty
```

**搜索知识库时添加过滤**：
```json
{
    "query": "{{file_path}}",
    "top_k": 20,
    "where": {"source": "{{file_path}}"}
}
```

---

## 四、前后端数据库设计要求

### 4.1 题目表 (questions)

```sql
CREATE TABLE questions (
    id VARCHAR(64) PRIMARY KEY,              -- 题目ID（UUID）
    question_type ENUM('choice', 'blank', 'short_answer') NOT NULL,
    content TEXT NOT NULL,                    -- 题干内容
    options JSON,                             -- 选择题选项（JSON数组）
    correct_answer TEXT NOT NULL,             -- 正确答案
    analysis TEXT,                            -- 解析
    knowledge_points JSON,                    -- 知识点（JSON数组）
    difficulty TINYINT DEFAULT 3,             -- 难度(1-5)
    score INT NOT NULL,                       -- 分值

    -- 溯源字段（核心）
    source_file VARCHAR(255) NOT NULL,        -- 来源文件路径
    source_collection VARCHAR(64) NOT NULL,   -- 来源向量库
    source_snippet TEXT,                      -- 来源知识片段
    source_hash VARCHAR(64),                  -- 文件哈希（用于检测文件变更）

    -- 审核状态
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
    reviewed_by VARCHAR(64),
    reviewed_at DATETIME,

    -- 元数据
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(64),
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_source_file (source_file),
    INDEX idx_source_collection (source_collection),
    INDEX idx_question_type (question_type),
    INDEX idx_status (status)
);
```

### 4.2 试卷表 (exams)

```sql
CREATE TABLE exams (
    id VARCHAR(64) PRIMARY KEY,              -- 试卷ID
    name VARCHAR(255) NOT NULL,              -- 试卷名称
    description TEXT,                         -- 描述
    total_score INT NOT NULL,                 -- 总分
    total_count INT NOT NULL,                 -- 题目总数
    duration INT DEFAULT 60,                  -- 考试时长（分钟）

    -- 状态
    status ENUM('draft', 'pending', 'published', 'archived') DEFAULT 'draft',
    published_at DATETIME,

    -- 元数据
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    created_by VARCHAR(64),
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_status (status)
);
```

### 4.3 试卷题目关联表 (exam_questions)

```sql
CREATE TABLE exam_questions (
    exam_id VARCHAR(64) NOT NULL,
    question_id VARCHAR(64) NOT NULL,
    question_order INT NOT NULL,              -- 题目顺序

    PRIMARY KEY (exam_id, question_id),
    FOREIGN KEY (exam_id) REFERENCES exams(id) ON DELETE CASCADE,
    FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE,

    INDEX idx_exam_id (exam_id),
    INDEX idx_question_id (question_id)
);
```

### 4.4 学生答卷表 (student_answers)

```sql
CREATE TABLE student_answers (
    id VARCHAR(64) PRIMARY KEY,
    exam_id VARCHAR(64) NOT NULL,
    student_id VARCHAR(64) NOT NULL,
    student_name VARCHAR(100),

    question_id VARCHAR(64) NOT NULL,
    question_type ENUM('choice', 'blank', 'short_answer') NOT NULL,
    student_answer TEXT NOT NULL,             -- 学生答案

    -- 批阅结果
    score INT DEFAULT 0,
    max_score INT NOT NULL,
    feedback TEXT,
    score_details JSON,                       -- 评分详情（JSON）

    -- 元数据
    submitted_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    graded_at DATETIME,

    FOREIGN KEY (exam_id) REFERENCES exams(id) ON DELETE CASCADE,
    FOREIGN KEY (question_id) REFERENCES questions(id) ON DELETE CASCADE,

    INDEX idx_exam_student (exam_id, student_id),
    INDEX idx_student_id (student_id)
);
```

### 4.5 批阅报告表 (grade_reports)

```sql
CREATE TABLE grade_reports (
    id VARCHAR(64) PRIMARY KEY,
    exam_id VARCHAR(64) NOT NULL,
    student_id VARCHAR(64) NOT NULL,
    student_name VARCHAR(100),

    total_score INT NOT NULL,
    max_score INT NOT NULL,
    score_rate DECIMAL(5,2),

    -- 整卷分析（可选）
    analysis JSON,                            -- AI生成的整卷分析

    graded_at DATETIME DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY (exam_id) REFERENCES exams(id) ON DELETE CASCADE,

    INDEX idx_exam_id (exam_id),
    INDEX idx_student_id (student_id)
);
```

---

## 五、文件修改联动流程

### 5.1 触发条件
当文件被修改或删除时，通过 `source_file` 字段查找受影响的题目。

### 5.2 联动逻辑（前后端实现）

```sql
-- 查找受影响的题目
SELECT id, source_file, source_hash
FROM questions
WHERE source_file = 'public/产品手册.pdf';

-- 如果文件哈希变更，标记题目需要重新审核
UPDATE questions
SET status = 'pending',
    source_hash = 'new_hash_value'
WHERE source_file = 'public/产品手册.pdf';
```

### 5.3 后端提供的联动接口

**接口路径**：`POST /exam/check-file-changes`

**请求参数**：
```json
{
    "file_path": "public/产品手册.pdf",
    "new_hash": "新的文件哈希"
}
```

**返回结果**：
```json
{
    "success": true,
    "file_path": "public/产品手册.pdf",
    "affected_questions": ["q_uuid_001", "q_uuid_002", ...],
    "count": 15,
    "recommendation": "建议重新生成该文件的题目"
}
```

---

## 六、实施步骤

### 阶段1：后端改造（1天）

1. **exam_manager.py**
   - 新增 `generate_exam_by_file()` 函数
   - 新增 `grade_from_mysql()` 函数
   - 修改 Dify 调用参数

2. **exam_api.py**
   - 新增 `/generate-by-file` 接口
   - 新增 `/grade-from-mysql` 接口
   - 新增 `/check-file-changes` 接口

3. **Dify工作流**
   - 修改输入参数
   - 添加文件过滤条件

### 阶段2：前后端对接（前后端并行）

1. **前端**
   - 创建MySQL表结构
   - 开发题目管理界面
   - 开发试卷组卷功能
   - 开发考试界面

2. **后端**
   - 测试新接口
   - 编写接口文档

### 阶段3：联调测试（0.5天）

1. 测试按文件出题流程
2. 测试批卷流程
3. 测试文件修改联动

---

## 七、接口文档汇总

### 7.1 出题接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/exam/generate-by-file` | POST | 按文件生成题目 |
| `/exam/by-file` | GET | 查询文件关联的题目 |

### 7.2 批卷接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/exam/grade-from-mysql` | POST | 基于MySQL题目批卷 |

### 7.3 联动接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/exam/check-file-changes` | POST | 检查文件变更影响的题目 |

---

## 八、注意事项

1. **题目ID生成**：使用UUID，确保全局唯一
2. **文件哈希**：用于检测文件变更，建议使用MD5或SHA256
3. **批量批卷性能**：简答题批卷耗时，建议使用异步处理或并发
4. **错误处理**：批卷失败时返回默认结果，不影响整体流程
